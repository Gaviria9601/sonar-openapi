openapi: "3.0.1"
info:
  version: 1.0.0
  title: Swagger Petstore
paths:
  /pets:
    get:
      responses: {}
    post:
      responses:
        default:
          description: the default response
  /stores:
    get:
      responses: { }
    post:
      responses:
        default:
          description: the default response
